# docker-compose.yml
version: '3.7'
services:
  jenkins:
    image: jenkins/jenkins:lts
    privileged: true
    user: root
    ports:
      - 8080:8080
      - 50003:50000
    container_name: jenkins
    volumes:
      - ~/jenkins_data:/var/jenkins_home
    restart: always
